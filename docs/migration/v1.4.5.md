# Migration to v1.4.5

From `v1.4.4` to `v1.4.5`.

## 1. Update the verification script locations

Update `src/AGENTS.md`:

```diff
-After changes, find and run `.llm/verify.{sh,ps1}` to test + lint.
-Print all output.
+After changes, run your platform's `verify.sh` or `verify.ps1` in `.llm/` or
+`src/.llm/` if present.
+
+It runs project verification, including tests and linting.
+Print all output; do not repeat checks it already runs.
```

## 2. Update the template version marker

```diff
-reloaded-templates-rust:1.4.4
+reloaded-templates-rust:1.4.5
```
